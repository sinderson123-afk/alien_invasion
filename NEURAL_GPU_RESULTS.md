# CUDA 扩容训练（2026-09-20）

## 环境与资源

检查发现此前的 PyTorch 是 `2.14.0+cpu`，没有使用 GPU。桌面已有约 2.3 GiB 显存占用，并非此前神经网络训练产生。

本次在 `%LOCALAPPDATA%\AlienInvasionPlay\gpu-venv` 安装独立的 `torch 2.14.0+cu130` 环境，实测 RTX 5070 Ti、16 GiB 显存、计算能力 12.0、BF16 和 CUDA 矩阵运算均可用。安装依照 [PyTorch 官方指引](https://docs.pytorch.org/get-started/locally/)，可复现依赖见 `requirements-training-cuda.txt`。原游戏的 CPU 环境继续保留。

## 已完成训练

| 分支 | 策略规模 | 真实练习决策 | 空间观测样本 | 训练日志用时 | 张量峰值 / 缓存峰值 |
| --- | --- | ---: | ---: | ---: | ---: |
| 从头训练的大网络 | 3×1024，2,237,464 参数，旧版约 66 倍 | 163,840 | 60,000 | 745 秒 | 260 / 364 MiB |
| 保留旧策略再扩宽 | 2×1024，1,187,864 参数，旧版约 35 倍 | 73,728 | 60,000 | 440 秒 | 219 / 306 MiB |

两分支合计 **237,568 次真实游戏决策、120,000 个空间练习样本**，执行约 142 万个基础游戏帧，另有正常过场帧。各分支 8 个 CPU 游戏进程，部分时间并行运行两组；GPU 负责集中推理、批量梯度更新。共完成 130 局自然结束的练习，训练预算结束时的其余进行中轨迹不作为完整对局成绩。

第一分支为 40 轮几何预训练、16 轮纠错模仿、64 轮 PPO；第二分支保留旧策略初始输出，不重做几何预训练，使用 4 轮带动作类别权重的模仿、32 轮 PPO。第二分支新增的空间样本参与回放与后续训练。监督批量最高 8,192，PPO 批量最高 2,048，保留梯度裁剪、KL 提前停止与监督回放约束。

各进程使用独立未登录临时档，只按普通输入游玩，死亡后重新建立新档。没有修改敌人、血量、金币、射速、掉率或正式存档，也没有上传训练分数。正式部署仍通过原文本接口控制游戏。

GPU 内存占用不是目标：更新期间所需张量本来就小，绝大部分时间仍消耗在 CPU 游戏模拟和进程通信上。单组实测约 230–300 次决策/秒；扩大模型没有让显存成为瓶颈。上述分配/缓存数字来自 PyTorch，包含桌面和驱动上下文的 `nvidia-smi` 总占用更高，不能混为同一指标。

## 候选筛选

所有候选使用验证种子 5100–5103，每局新档、最多 6,000 次决策。表中均自然死亡，没有截断。验证集用于选模型和执行方式，不能再充当最终留出评测。

| 候选 | 执行方式 | 平均分 | 平均金币收入 |
| --- | --- | ---: | ---: |
| 原 `pilot-tactics.dat` | 最大概率动作 | 6,693.75 | 31.25 |
| 大网络，模仿第 16 轮 | 最大概率动作 | 5,100 | 22.5 |
| 大网络，第 32 轮（16 轮 PPO） | 最大概率动作 | 5,475 | 27 |
| 大网络，第 48 轮（32 轮 PPO） | 最大概率动作 | 5,150 | 19.25 |
| 大网络，第 64 轮（48 轮 PPO） | 按概率采样 | 6,931.25 | 35 |
| 大网络，第 80 轮（64 轮 PPO） | 按概率采样 | 7,606.25 | 38.25 |
| 扩宽网络，模仿第 4 轮 | 最大概率动作 | **10,487.5** | **51.25** |
| 扩宽网络，第 36 轮（32 轮 PPO） | 最大概率动作 | 7,462.5 | 38.25 |
| 扩宽网络，第 36 轮（32 轮 PPO） | 按概率采样 | 8,300 | 34 |

因此选出 `pilot-gpu-widen-imitation-4.dat` 进入最终留出评测。增加 PPO 轮数没有持续改善表现，不能仅因训练更久或网络更大就替换模型。从头训练分支丢失了较多购物行为；保留旧策略再扩宽，并加权少见动作的分支恢复了购买升级和使用磁铁的能力。

独立空间观测检查使用种子 91000、每类 500 个场景，选中候选的最大概率动作正确率为：单个静止敌人 98.8%、移动敌人 99.0%、纯掉落物 95.2%、敌人和掉落物并存 95.4%、近身危险 93.2%。这是相对于示范标签的动作检查，不是对局胜率。

## 最终留出评测与选择

候选和执行方式固定之后，使用未参与训练或候选筛选的 6100–6107 八个种子，各自新档，与原模型对照。两边 16 局均自然结束，没有截断。

| 指标 | 原模型 | 选中扩宽候选 |
| --- | ---: | ---: |
| 平均分 | **8,403.125** | 8,184.375 |
| 最高分 | 11,550 | 12,725 |
| 平均金币收入 | 38.375 | 38.75 |
| 安全低位掉落物出现时朝其移动的比例 | 52.3% | 37.1% |
| 单目标未对准仍停住的步数比例 | 16.7% | 17.1% |

| 种子 | 原模型得分 | 扩宽候选得分 |
| --- | ---: | ---: |
| 6100 | 7,700 | 11,200 |
| 6101 | 11,550 | 8,875 |
| 6102 | 6,600 | 12,725 |
| 6103 | 6,600 | 6,650 |
| 6104 | 7,775 | 10,275 |
| 6105 | 9,975 | 8,150 |
| 6106 | 10,425 | 1,450 |
| 6107 | 6,600 | 6,150 |

**不替换当前 `champion.dat`。** 新候选有更高的单局分数，但平均分低约 2.6%，且出现仅 1,450 分的早期失败，最终留出集没有确认验证集上的优势。金币收入基本持平，直接追逐掉落物的比例下降；这也提醒我们，增加网络容量不能自动保持之前的行为改进。停住指标包含为拾取/避险暂停追敌等情况，单独不能当成纯瞄准错误率。

旧 `champion.dat` 与训练前冻结的 `pilot-gpu-source.dat` 文件哈希一致。新候选和所有 PPO 检查点均保留用于后续研究；不根据这组留出种子继续调参后再把它称为未见测试。这里的分数全部是离线评测，**没有上传排行榜或更新正式账号纪录**。所有训练与评测进程已结束。

## 运行与验证

17 项自动测试通过，包含多进程独立移动/退出、GAE 不跨越死亡边界、网络扩宽后初始动作与价值输出保持一致、旧/新模型格式兼容，以及原有文本控制和商店约束。

选中候选在原 CPU 环境、单线程下，预热后 200 次前向计算中位数约 0.079 ms，P95 约 0.086 ms。该数字只测网络前向，不包含观察编码、HTTP 或游戏绘制。

运行命令与执行方式说明见 [NEURAL_PILOT.md](NEURAL_PILOT.md)。全部权重、日志仍在忽略目录 `training_runs/`，代码与结果说明纳入 Git。主要日志：`train-gpu.log`、`train-gpu-widen.log`、`eval-gpu-*-validation.log`、`eval-gpu-baseline.log`、`eval-gpu-selected-holdout.log`、`gpu-tests.log`。
